# This is a basic workflow that is manually triggered

name: Build only on index.html change

on:
  push:
    paths:
      - 'index.html'
  pull_request:
    paths:
      - 'index.html'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📂 Show changed files (debug)
        run: |
          echo "Changed files:"
          git diff --name-only ${{ github.event.before }} ${{ github.sha }}

      - name: ⚙️ Run Build
        run: |
          echo "✅ Build triggered because index.html changed"
          # 👉 Replace below with your actual build command
          # Example:
          # npm install
          # npm run build

